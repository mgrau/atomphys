name: Run Notebook Validation
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
      - name: Install pytest
        run: pip install pytest nbval numpy matplotlib
      - name: Install atomphys
        run: pip install .
      - name: Test
        run: pytest --nbval docs/
